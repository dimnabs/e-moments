services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: emoment
      POSTGRES_USER: emoment
      POSTGRES_PASSWORD: emoment_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U emoment -d emoment"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: emoment
      MINIO_ROOT_PASSWORD: emoment_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
